#!/bin/bash
if [ "$KNAPSACK_PRO_TEST_SUITE_TOKEN_RSPEC" = "" ]; then
  bundle exec rake knapsack:rspec # use Regular Mode here always
else
  # Queue Mode - dynamic tests allocation across CI nodes
  bundle exec rake knapsack_pro:queue:rspec
fi
