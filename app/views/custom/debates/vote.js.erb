$("#<%= dom_id(@debate) %>_votes").html('<%= j render("debates/votes", debate: @debate) %>');
<% if user_signed_in? && @debate.voted_on_by?(current_user) %>
  $("#debate-comment").html('<%= j render("comments/form", {commentable: @debate, parent_id: nil, toggeable: false}) %>');
<% end %>
