<header>

  <%= render "shared/top_links" %>

  <div class="row">
    <div class="top-bar">

      <%= link_to setting['org_name'], root_path, class: "logo show-for-small-only" %>

      <span data-responsive-toggle="responsive-menu" data-hide-for="medium" class="float-right">
        <span class="menu-icon dark" data-toggle></span>
        <%= t("application.menu")%>
      </span>

      <div id="responsive-menu">
        <div class="top-bar-title medium-9 small-12">
          <%= link_to root_path, class: "hide-for-small-only", accesskey: "/" do %>
            <%= image_tag('participaGc_header.png', class: 'hide-for-small-only float-left', alt: t("layouts.header.logo")) %>
            <%# <%= image_tag('Logotipo Participa.svg', class: 'hide-for-small-only float-left', alt: t("layouts.header.logo")) %>
            <% if opendata_page? %>
              <%= t("layouts.header.open_gov", open: "#{t('layouts.header.open')}") %> <span>|</span>
              <span class="logo-site"><%= t("layouts.header.open_data") %></span>
            <% end %>
          <% end %>
        </div>

        <div class="top-bar-right small-12 medium-3">
          <ul class="menu">
            <%= render "devise/menu/login_items" %>
          </ul>

          <div class="show-for-small-only">
            <div class="subnavigation">
              <%= render "shared/subnavigation" %>
              <div class="small-12 medium-3 column">
                <%= yield :header_addon %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="subnavigation expanded">
    <div class="row">
      <div class="hide-for-small-only">
        <%= render "shared/subnavigation" %>
      </div>
    </div>
  </div>

</header>
